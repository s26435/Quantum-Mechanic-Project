# Projekt na przedmiot Mechanika Kwantowa
OPIS

jak odpalić pierwszy raz:
```sh
mkdir build
cmake -S . -B build
cmake --build build -j
```

potem jak masz folder build to:
```sh
cmake --build build -j
```